import RPC from './RPC';
import { Message, RpcConfig } from './types';
declare class PortRPC extends RPC {
    private port;
    constructor(config: RpcConfig);
    protected sendMessage(message: Message): void;
    private receiveMessage;
    start(): void;
    stop(): void;
}
export default PortRPC;
