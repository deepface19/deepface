import RPC from './RPC';
import { Message, RpcConfig, ReqMessage } from './types';
declare class ProxyRPC extends RPC {
    private readonly win;
    private port;
    constructor(win: Window, config: RpcConfig);
    private proxyResponse;
    private sendPortMessage;
    protected sendMessage(message: Message): void;
    private receiveMessage;
    protected onRequestMessage(message: ReqMessage): void;
    start(): void;
    stop(): void;
}
export default ProxyRPC;
