"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateMessageSchema = void 0;
const schema_1 = __importDefault(require("./schema"));
const validateMessageSchema = (message) => {
    const result = schema_1.default.validate(message);
    if (result.error)
        return { isValid: false, type: null };
    let type = null;
    if (message.data.data.hasOwnProperty('method')
        && message.data.data.hasOwnProperty('params')) {
        type = 'req';
    }
    else if (message.data.data.hasOwnProperty('result')
        || message.data.data.hasOwnProperty('error')) {
        type = 'res';
    }
    return {
        type,
        isValid: (type === 'req' || type === 'res'),
    };
};
exports.validateMessageSchema = validateMessageSchema;
